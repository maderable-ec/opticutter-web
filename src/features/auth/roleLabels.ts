import type { Role } from './types'

/** Human-readable labels for the Spanish role values. */
export const ROLE_LABELS: Record<Role, string> = {
  administrador: 'Administrador',
  vendedor: 'Vendedor',
  operador: 'Operador',
  canteador: 'Canteador',
}
